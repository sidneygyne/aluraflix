// import styles from './ModalPlay.module.css'

function ModalPlay({ link }) {
    return (

        <dialog open={true} >
            <form method="dialog">
                
                <button type="submit">ok</button>
            </form>
        </dialog>

    )
}

export default ModalPlay